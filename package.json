{
  "name": "ferry-express",
  "version": "1.0.0",
  "description": "Express router adapter for FerryJS.",
  "license": "MIT",
  "main": "lib/Express",
  "scripts": {
    "test": "mocha",
    "build": "babel src --out-dir lib --source-maps",
    "watch": "babel src --out-dir lib --watch --source-maps",
    "postinstall": "npm run build",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "express": "^4.12.3",
    "ferry": "ferryjs/ferry#development"
  },
  "devDependencies": {
    "babel": "^4.7.16"
  }
}
